name: ci

on:
  push:
    tags-ignore:
      - "v*.*.*"
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build
        run: make

      - name: Smoke test
        run: ./bin_cs test_hello.c
